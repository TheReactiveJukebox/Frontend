stages:
- build

frontend:
  stage: build
  # https://hub.docker.com/_/node/
  image: node:alpine
  cache:
    key: "frontend_npm"
    paths:
    - /root/.npm/_cacache/
  script:
    - npm install
    - npm run build:aot:prod
